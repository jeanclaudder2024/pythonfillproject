services:
  - type: web
    name: autofill-api
    env: python
    buildCommand: chmod +x build.sh && ./build.sh
    startCommand: uvicorn working_fastapi:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
